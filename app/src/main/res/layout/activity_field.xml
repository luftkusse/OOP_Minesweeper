<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageButton
        android:id="@+id/imageButtonField"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:background="@drawable/field_close"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:isScrollContainer="false"
        android:scaleType="center" />

    <TextView
        android:id="@+id/textViewCountBombsAround"
        android:layout_width="25dp"
        android:layout_height="25dp"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/imageButtonField"
        app:layout_constraintEnd_toEndOf="@+id/imageButtonField"
        app:layout_constraintStart_toStartOf="@+id/imageButtonField"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>